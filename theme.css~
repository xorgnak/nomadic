body { text-align: center; font-family: monospace; margin: 0; overflow: hidden; }
#canvas {
  position: absolute;
  margin: 0px;
  padding: 0px;
  background-color: #D5D18A;
  left: 0;
  z-index: -100;
}
button { width: 100%; }
#head { background-color: black; margin: 0; padding: 4px; }
.head_item { background-color: white;
	     border-radius: 10px;
	     padding: 2px;
	   }
.head_icon {
    padding: 0px 1px 0px 1px;
}
#rank { font-size: x-large;
border: solid black;
border-radius: 0px 0px 10px 10px;
margin: 0 auto 0 auto;
padding: 3px;
width: 95%;
      }
.rank {
    padding: 3px; margin: 1px;
}
#end {
    position: fixed;
    left: 12.5%;
    bottom: 5%;
    width: 75%;
    border: thick solid darkred;
    border-radius: 10px;
    box-shadow: 0px 4px 21px black;
    font-size: xx-large;
    background-color: silver;
    color: black;
}
#stats { height: 20px; }
#experience {
    float: left;
}
#sortees {
    float: right;
}
.msg {
background-color: black;                                                                                                                                                                                             
color: orange;                                                                                                                                                                                                       
padding: 1px 0px 2px 5px;                                                                                                                                                                                            
margin: 10px;                                                                                                                                                                                                        
border-radius: 10px;
}
.choice {
	 }
.event {
    border: solid grey; border-radius: 10px;
}
.comment { text-align: left; }
.choice_choice { padding: 2px; margin: 2px; width: auto; }
.choice_message { font-size: medium; }
.comment_icon { font-size: large; margin: 2px; padding: 2px; }
.comment_message { font-size: medium; }
.event_icon { font-size: xx-large; }
.event_message { font-size: large; }
